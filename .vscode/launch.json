{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension + LSP Server",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/client"
      ],
      "preLaunchTask": "build",
      "outFiles": [
        "${workspaceFolder}/client/out/**/*.js"
      ]
    }
  ]
}
